# Working Doc Skeleton

位置：`docs/testing/YYYY-MM-DD-<slug>.md`（日期 = 開始規劃那天，之後不改檔名）。
用不到的段落刪掉，不要留 "TBD"。

---

```markdown
# <功能名稱>

> Working doc。隨討論即時更新，作為 session 之間接續的依據。
> 🎯 最終目標：每個 task self-contained，拿到單一 task 就能實作到測試綠。

## 🚀 接續備忘（下次打開先看這）

- **上次停在**：<一段話>
- **下次先做**：<1–3 條，照目標排，不是照 task 編號>
- **在等**：<等使用者決定 / 等實機 / 等權限 …，附影響的 task>
- **今天就能做**：<task IDs>

| 用途 | 路徑 |
| --- | --- |
| Branch | `<type>/<slug>` |
| PR | #<N>（功能做完才開） |
| 主要頁面 | `lib/main/pages/<...>` |
| Cubit | `lib/domain/bloc/<...>` |

## 需求原話

> <使用者原文，逐字貼上；之後的驗收對照這段>

## 範圍

- ✅ <IN>
- ❌ <OUT>

## 差異清單（需求 vs 現況）

- **D1** <主題>：需求 … / 現況 … → <判讀，狀態 ✅ / ⏸>

## 資料與狀態設計

```
路由參數 → BlocProvider → <Cubit> → <Repository> → <collection/欄位>
```

- 新欄位 / 舊資料相容規則
- 失敗時保留什麼（草稿、原頁）
- 每個非平凡決定的 source of truth（`file:line`）

## Task list

依賴：`A1 → B1 → C1`、`D1` 最後

### A1 — <標題>
- Status: ✅ Ready / ⏸ 待確認-<主題>
- Depends on: —
- 為什麼：<一句>
- 要做什麼：
  - …
- 驗收：
  - …
- 相關檔案：`…`
- 複雜度：S / M / L
- 🧪 測試：`test/<...>_test.dart` 的 <case 名>
- 🧪 手動：W1、W2

## 完整例子（Phase 7.4）

前狀態 → 操作 → 每一步（對應測試）→ 結果 → 誠實帳（涵蓋 / 沒涵蓋）

## 手動 test case

| # | 環境 | 驗證什麼 |
| --- | --- | --- |
| W1 | Web 1194×834 | … |

每條的操作步驟、預期畫面、前置條件寫在表格下方。

## 待確認

- [ ] <問題>（影響 A1、C1）

## 偏離紀錄

<跳過的步驟、換掉的方法、只驗了一部分的地方；沒有就寫「無」>

## 驗證結果

<實際跑了哪些指令、數字、沒驗到的部分>

## 變更紀錄

| 日期 | 內容 |
| --- | --- |
| YYYY-MM-DD | 初版 |
```
