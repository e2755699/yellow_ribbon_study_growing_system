# 2026-09-25 隱私政策與正式送審準備

從使用者測過的 build 3 後續提交 `9cd29bd` 建立 `codex/ipad-appstore-privacy`。原工作區的未提交變更未併入本次發布。

## 已確認的營運資訊

- 協會及合作人員專用，管理員建立帳號，App／帳號／功能皆免費。
- 學生停止接受服務後保留資料 1 年，期滿由管理員分別處理各類紀錄。App 沒有自動到期清除功能。
- 資料查詢、更正及刪除由服務負責人透過 e2755699@gmail.com 受理。
- 使用者確認目前學生資料全是假資料。Apple 審查沿用 appletest@gmail.com，不使用負責人的個人帳號。此事不代表 Firebase 權限隔離已完成。

## 政策與 UI

- 政策文字來源：`assets/jsons/privacy_policy.json`。`python tool/generate_privacy_policy.py` 產生離線 Dart 內容與 `docs/privacy-site/index.html`；Dart 產物再執行 dart format。
- 公開政策已於 GitHub Pages 發布並於瀏覽器核對全文：<https://e2755699.github.io/yellow_ribbon_study_growing_system/>。獨立 gh-pages 分支只包含 HTML 與 .nojekyll，不包含程式碼或學生資料。
- 登入頁及首頁新增 PrivacyPolicyButton；PrivacyPolicyView 使用 SystemPage／SystemSectionCard 和 SystemTheme。離線全文不依賴登入或 Firebase，返回保留原登入表單。
- Widgetbook 直接使用兩個正式元件，共 3 個案例；靜態離線文件不具有網路 loading／empty／error 狀態。按鈕另外展示 disabled 狀態。
- 本次僅新增政策 UI 及兩處入口；沒有宣稱登入頁、首頁或整個 App 已完整遷移設計系統。既有每日出席、表現、報表等 legacy 範圍保留。
- `tool/store_preview.dart` 與 integration_test 是獨立截圖驗證入口，組裝相同正式元件及合成資料，不被正式 main.dart 引用，不會向 Firebase 寫入資料。

## 驗證

- 新增政策 12 項測試通過：五種指定尺寸的 Light／Dark、離線閱讀／返回、登入表單保留、鍵盤啟動及 disabled。
- `tool/check_design_system.ps1` 通過：App 108 項、Widgetbook 23 項；目錄由 build_runner 產生且一致，Widgetbook 分析無問題，原遷移範圍僅 4 個既有 info。
- 新政策元件、內容與截圖 harness 靜態分析無問題。

## 發布狀態

預定版本為 1.0.0+4。Codemagic 沿用既有 Workflow Editor、Apple 整合、自動簽章及原發布帳號。新增 post-build 腳本僅在分支包含截圖腳本時執行，以原生 13 吋 iPad 模擬器取得截圖，並輸出到 CM_EXPORT_DIR。

App Store Connect 的隱私政策 URL 已儲存為上述 GitHub Pages 網址。

Codemagic build `6ab64d68c730ccb654c9fe56` 正在執行，來源提交 `9dd3db7`。雲端建置、截圖、build 選擇及正式提交仍須以實際結果更新，不把本地測試或政策網站發布當作 App 已上架。

## 審查帳號驗證

2026-09-25 使用 App Store Connect 既有審查帳號向 Firebase 官方 signInWithPassword API 驗證成功（registered=true，UID 與既有 Apple reviewer 相符）；回傳 token 未保存或輸出。未變更密碼，未使用負責人的個人帳號。

同日 localhost Web UI 登入顯示 network-request-failed；因此只確認帳密及 Firebase 帳號有效，沒有把網頁 UI 登入或新 build 的原生登入標示為通過。
